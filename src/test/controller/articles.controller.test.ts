import *  as supertest from 'supertest';
import { getArticle } from '@components/article/controller';
// import { logger } from '@config/logger';
describe('User API', () => {
  let request = null;
  beforeAll( () => {
    // request = supertest(server.callback());
  });
  afterAll( () => {

   // server.close();
  });

  it('Article get request',   () => {

  });
})

