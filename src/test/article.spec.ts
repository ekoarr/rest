import *  as supertest from 'supertest';
import server from '../index';
 describe('User API', () => {
//   let request = null;
//   beforeAll( () => {
//      request = supertest(server.callback());
//   });
//   afterAll( () => {

//   });
  
//   const throwIfError = (err, res) => { if (err) throw err }
  it('Article get request',   () => {
//      request
//       .get(`/article`)
//       .expect(200)
//       .end((err, res)=>{
//         if (err) return done(err);
//         done();
//       })
  })
 });